// Public constants for asset URLs used by the UI.
// Keep these small and easy to override in tests or during SSR.
export const fx_LOGO_URL = "/logo.png";

// White variant for dark backgrounds (kept separate for clarity)
export const fx_LOGO_WHITE_URL = "/logo.png";
