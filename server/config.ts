// Simple configuration values used by the server-side routes.
// In production, override `ADMIN_PASSWORD` via environment variables.
export const ADMIN_PASSWORD = process.env.ADMIN_PASSWORD || "admin123";
